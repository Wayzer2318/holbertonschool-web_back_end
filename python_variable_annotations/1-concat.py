#!/usr/bin/env python3
"""concat"""


def concat(str1: str, str2: str) -> str:
    """function that takes a str1 and
     str2 and returns a concatenated string"""
    return "{}{}".format(str1, str2)
